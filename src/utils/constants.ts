/**
 * The diff did not find the property in the second object.
 */
export const Missing = Symbol('Missing');

/**
 * The diff found that both properties have the same value.
 */
export const Same = Symbol('Same');
